spring:
  application:
    name: veganing
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

scheduler:
  recipe-cron: "0 0 0 * * *"