spring:
  application:
    name: veganing
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

scheduler:
  recipe-cron: "0 0 0 * * *"