package com.veganing;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class VeganingApplicationTests {
	// Spring Initializr 기본 생성 파일
	// 전체 컨텍스트 로딩 테스트는 로컬에서만 가능 (application-local.yaml 필요)
	// CI/CD 환경에서는 제외
}
